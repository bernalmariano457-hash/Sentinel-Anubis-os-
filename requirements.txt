# AnubisOS / Apex Sentinel — Dependencias
# Instalar con: pip install -r requirements.txt
# En Termux:    pip install -r requirements.txt --break-system-packages

# --- Interfaz de terminal ---
rich>=13.7.0

# --- Seguridad y autenticación ---
bcrypt>=4.1.2
cryptography>=42.0.0        # Fernet (Security.py)
pycryptodome>=3.20.0        # AES-256-GCM (WADecryptor.py)

# --- Red y escaneo ---
scapy>=2.5.0
requests>=2.31.0
flask>=3.0.0                # EvilTwinServer.py, PhishingModule.py

# --- Procesamiento de señales (RF) ---
numpy>=1.26.0               # RFScanner.py, dsp.py

# --- Análisis de imágenes / EXIF ---
Pillow>=10.2.0
piexif>=1.1.3

# --- Bluetooth BLE ---
bleak>=0.21.1               # BLE moderno (reemplaza PyBluez abandonado)

# --- Generación de reportes ---
fpdf2>=2.7.9                # MotorReportes.py (PDFs)

# --- Mapas y geolocalización ---
folium>=0.16.0              # GeomapSentinel.py (mapas HTML interactivos)

# --- Utilidades generales ---
colorama>=0.4.6             # Compatibilidad colores en Windows
psutil>=5.9.8               # Info de sistema (CPU, RAM, red)

# --- Testing ---
pytest>=8.0.0

# --- Opcionales (hardware SDR) ---
# pyrtlsdr>=0.3.0           # RTL-SDR (descomentar si tienes el hardware)
# SoapySDR                  # HackRF y otros SDR (instalar desde sistema)
